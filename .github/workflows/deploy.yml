name: Deploy

on: 
 workflow_call:

jobs:
  build:
   runs-on: ubuntu-latest
   steps:
     - name: Code clone
       uses: actions/checkout@v7
       
     # - name: Deploy via nginx
     #   run: cp index.html /var/www/html

     - name: Deploy the code
       run: echo " Code deployed to the AWS EC2 Instance"
